trigger:
  - main
pool:
  vmImage: ubuntu-latest
steps:
  - task: NodeTool@0
    inputs:
      versionSpec: '20.x'
  - script: |
      corepack enable
      pnpm install
      pnpm test
    displayName: 'SETwin tests'
